const  decimal2 = Number(prompt("Insira o primeiro numero"));
const decimal = Number(prompt("Insira mais um numero:"));

//processamento
const soma = decimal+decimal2;
console.log(`A soma é ${soma.toFixed(2)}`);
